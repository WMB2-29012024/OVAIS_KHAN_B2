// Use unshift method to add new data in the array, and log the new length of the array, but without using the .length property.
const elements = [1,2,3]
const lenght=elements.unshift(0);
console.log(elements,lenght);